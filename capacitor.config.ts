import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pftscorecalculator.app',
  appName: 'pft-score-calculator',
  webDir: 'out'
};

export default config;
